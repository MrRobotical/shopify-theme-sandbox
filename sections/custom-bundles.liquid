<style>
  #bundles-form {
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
  }
  .bundle-group {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin-bottom: 20px;
  }
  .bundle-container {
    display: flex;
    justify-content: center;
    gap: 40px;
    margin-bottom: 20px;
  }
  .bundle-group img {
    max-width: 150px;
    margin-bottom: 10px;
  }
  .bundle-group h3 {
    margin-bottom: 5px;
  }
  .bundle-group span {
    margin-bottom: 10px;
    font-size: 18px;
    font-weight: bold;
  }
  #submit-button {
    padding: 10px 20px;
    background-color: #007bff;
    color: white;
    border: none;
    cursor: pointer;
  }
</style>

<form id="bundles-form">
  <h2>Snowboard Bundle</h2>
  <div class="bundle-container">
    <div class="bundle-group">
      <!-- Hard-coded product 1 details -->
      <img src="https://cdn.shopify.com/s/files/1/0001/7647/9621/products/regular_snow.jpg?v=1592562542" alt="Regular Snow">
      <h3>Regular Snow</h3>
      <span>$199.99</span>
    </div>
    <div class="bundle-group">
      <!-- Hard-coded product 2 details -->
      <img src="https://cdn.shopify.com/s/files/1/0001/7647/9621/products/snowboard_boots.jpg?v=1592562542" alt="Snowboard Boots">
      <h3>Snowboard Boots</h3>
      <span>$199.99</span>
    </div>
  </div>
  <button id="submit-button">Add Bundle to Cart</button>
</form>

<script>
const form = document.getElementById("bundles-form");

form.addEventListener("submit", async (e) => {
  e.preventDefault();
  
  // Hard-coded variant IDs
  const group1 = '8514400387308';
  const group2 = '8454485311724';

  const bundleId = new Date().getTime();

  await fetch("/cart/add.js", {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      items: [
        {
          id: group1,
          quantity: 1,
          properties: {
            _bundleId: bundleId
          }
        },
        {
          id: group2,
          quantity: 1,
          properties: {
            _bundleId: bundleId
          }
        }
      ]
    })
  });

  window.location.reload();
});
</script>

{% schema %}
{
    "name": "Custom Bundle Builder4",
    "settings": [],
    "presets": [
        { 
            "name": "Custom Bundle Builder4"
        }
    ]
}
{% endschema %}






{% comment %} <form id="bundles-form">
  <div>
    <label for="group-1">Group 1</label>
    <select id="group-1" name="group-1">
      {% for product in collections.all.products %}
        {% if product.tags contains "bundle1" %}
          {% assign variant = product.selected_or_first_available_variant %}
          <option value="{{ variant.id }}">{{ product.title }} - {{ variant.price | money }}</option>
        {% endif %}
      {% endfor %}
    </select>
  </div>

  <div>
    <label for="group-2">Group 2</label>
    <select id="group-2" name="group-2">
      {% for product in collections.all.products %}
        {% if product.tags contains "bundle2" %}
          {% assign variant = product.selected_or_first_available_variant %}
          <option value="{{ variant.id }}">{{ product.title }} - {{ variant.price | money }}</option>
        {% endif %}
      {% endfor %}
    </select>
  </div>

  <button>Submit</button>
</form>

<script>
const form = document.getElementById("bundles-form");

form.addEventListener("submit", async (e) => {
  e.preventDefault();
  
  const group1 = document.getElementById("group-1").value;
  const group2 = document.getElementById("group-2").value;

  const bundleId = new Date().getTime();

  await fetch("/cart/add.js", {
    method: "POST",
    headers: {
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      items: [
        {
          id: group1,
          quantity: 1,
          properties: {
            _bundleId: bundleId
          }
        },
        {
          id: group2,
          quantity: 1,
          properties: {
            _bundleId: bundleId
          }
        }
      ]
    })
  });

  window.location.reload();
});
</script>



{% schema %}
{
    "name": "Custom Bundle Builder",
    "settings": [
        
        ], 
        "presets": [
            { 
                "name": "Custom Bundle Builder"
            }
           
        ]
}
{% endschema %} {% endcomment %}